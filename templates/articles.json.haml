[
- @articles.each do |article|
  {
  "title":       "#{article.title}",
  "url":         "#{article.web_url}",
  "date":        "#{article.date}",
  "locale_date": "#{article.locale_date}",
  "summary":     "#{article.summary}"
  }
]